import React from 'react';
import { StyleSheet } from 'react-native';

const ContainerBaseStyle = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default ContainerBaseStyle;